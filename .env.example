# Required Command Arguments
# Your wallet's private key (required)
WALLET_PRIVATE_KEY="your-private-key-here"

# Token mint address to trade (required)
TOKEN_MINT_ADDRESS="token-mint-address-here"

# Your wallet address (required)
WALLET_ADDRESS="your-wallet-address-here"

# Trading Parameters
# Percentage of wallet balance to use for each buy (0.1 = 10%)
BUY_PERCENTAGE=0.1

# Minimum buy amount in SOL to prevent very small trades
MIN_BUY_AMOUNT=0.01

# Maximum number of retry attempts for failed transactions
MAX_RETRIES=3

# Trading intervals configuration (in seconds)
MIN_INTERVAL=2
MAX_INTERVAL=15
DEFAULT_INTERVAL=10

# Market activity thresholds
VOLUME_THRESHOLD=1000
PRICE_CHANGE_THRESHOLD=0.05

# Transaction Parameters
# Slippage tolerance in basis points (100 = 1%)
SLIPPAGE_BASIS_POINTS=100

# Trading limits
UNIT_LIMIT=250000
UNIT_PRICE=250000

# The amount in Solana to add as priority fee for each transaction
# In tests, priority fees were not needed
PRIORITY_FEE=0

# Solana RPC Configuration
# Helius RPC endpoint - Get your API key from https://dev.helius.xyz/
# Format: https://mainnet.helius-rpc.com/?api-key=YOUR-API-KEY
RPC_URL="https://mainnet.helius-rpc.com/?api-key=YOUR-API-KEY"

# Note: Dynamic buy amounts are now calculated based on wallet balance
# The old BUY_AMOUNT and BUY_INTERVAL settings are replaced with adaptive logic